streamlit>=1.25.0
pandas>=1.5.3
numpy>=1.24.2
plotly>=5.15.0
requests>=2.31.0

# Statistical functions
scipy==1.14.1

# Optional but recommended for better performance
# pyarrow==17.0.0  # For faster parquet operations if needed

# Note: These are the latest stable versions as of deployment
# Streamlit Cloud will automatically install these dependencies

seaborn>=0.12.2


xlsxwriter==3.1.9

openpyxl # for Excel export if not already there
python-dateutil # for datetime operations if not implicitly covered
dataclasses # if using Python < 3.7
functools # if using Python < 3.2
typing # if using Python < 3.5
